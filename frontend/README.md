# Frontend

Next.js (App Router) + TypeScript + Tailwind CSS dashboard for the RAG
Document QA system. See the [project README](../README.md) for full setup,
architecture, and usage instructions.

```bash
npm install
cp .env.local.example .env.local
npm run dev
```
